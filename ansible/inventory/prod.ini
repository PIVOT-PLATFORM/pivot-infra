[prod]
# ansible_host: fill in with `terraform output external_ip` from environments/prod once that
# VM is provisioned (terraform apply — not yet run, see environments/prod/README references).
pivot-prod-vm ansible_host=CHANGEME ansible_user=pivot-deploy ansible_ssh_private_key_file=~/.ssh/pivot_prod_deploy
