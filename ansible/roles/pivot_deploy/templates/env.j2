TLS_CERT_PATH={{ pivot_deploy_path }}/tls/pivot.crt
TLS_KEY_PATH={{ pivot_deploy_path }}/tls/pivot.key

PIVOT_UI_VERSION={{ pivot_ui_version }}

SMTP_HOST={{ vault_smtp_host }}
SMTP_PORT={{ smtp_port }}
SMTP_USERNAME={{ vault_smtp_username }}
PIVOT_MAIL_FROM={{ pivot_mail_from }}

PIVOT_APP_URL={{ pivot_app_url }}
CORS_ALLOWED_ORIGINS={{ cors_allowed_origins }}
